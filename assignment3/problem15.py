'''
Write A function which receives a string statement as input parameter
which contains only alphabet variables and binary operations
including+, -, *, / and % and check whether the statement is a valid arithmetic statement or not.

The statement might contain parenthesis as well. For instance:
• a+b*a+c/c%y
• (a+b)*(a/d-(a/b))

You can make this assumption that the variable names contain only one alphabet(like a, b, c)
and cannot have more than one alphabets (like ab, temp, sum,....)


文字列ステートメントを入力変数として受け取り、アルファベット変数と、
+、 - 、*、/、および％を含む2項演算のみを含む関数を作成し、
そのステートメントが有効な算術ステートメントであるかどうかを確認します。

文に括弧も含まれる場合があります。 例えば：
• a+b*a+c/c%y
• (a+b)*(a/d-(a/b))

変数名に含まれるアルファベットは1つのみ（a、b、cなど）で、
複数のアルファベットを含めることはできない（ab、temp、sumなど）と仮定することができます。


'''